CREATE TABLE Customers (
	id int,
	name varchar(255),
	city varchar(255),
	country varchar(255)
);

CREATE TABLE Products (
	id int,
	name varchar(255),
	price varchar(255),
	count int
);




